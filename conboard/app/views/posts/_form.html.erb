<!-- edit 과 new의 중복 코드 한 번에 유지하기 위해 -->
<!-- <%= form_for @post do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :content %>
  <%= f.submit %>
<% end %> -->

<!-- simple_form으로 bootstrap 적용 하기 위해 변경된 code -->
<%= simple_form_for @post do |f| %>
  <%= f.input :title %>
  <%= f.input :content %>
  <%= f.button :submit, class: "btn-primary"%>
<% end %>
